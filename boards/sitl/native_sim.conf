CONFIG_TARGET_SITL=y

# ========== Native Simulator Configuration ==========
# Minimal configuration for testing on native_sim
# Hardware-specific features are excluded

# POSIX API for better x86 compatibility
CONFIG_POSIX_API=y