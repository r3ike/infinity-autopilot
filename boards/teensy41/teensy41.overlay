/ {
    chosen {
        zephyr,console = &cdc_acm_uart0;
        zephyr,shell-uart = &cdc_acm_uart0;
    };


    imu_0: bmi088-imu-0 {
        compatible = "infinity-autopilot,bmi088-imu";
        accel = <&bmi088_accel_0>;
        gyro = <&bmi088_gyro_0>;
    };
};

&zephyr_udc0 {
    cdc_acm_uart0: cdc_acm_uart0 {
        compatible = "zephyr,cdc-acm-uart";
        status = "okay";
    };
};

&lpi2c1 {
    pinctrl-0 = <&pinmux_lpi2c1>;
    pinctrl-names = "default";
    status = "okay";
    clock-frequency = <100000>;

    bmi088_gyro_0: bmi088-gyr@68 {
        compatible = "bosch,bmi08x-gyro";
        reg = <0x68>; /* Indirizzo I2C del giroscopio */
        int-gpios = <&gpio1 13 GPIO_ACTIVE_HIGH>;
        gyro-hz = "1000_116"; 
        gyro-fs = <2000>;
        status = "okay";

        int3-4-map-io = <0x01>;   /* abilita data-ready su INT3 */
        int3-4-conf-io = <0x0A>;
    };

    bmi088_accel_0: bmi088-acc@18 {
        compatible = "bosch,bmi08x-accel";
        reg = <0x18>; /* Indirizzo I2C dell'accelerometro */
        int-gpios = <&gpio1 12 GPIO_ACTIVE_HIGH>;
        accel-hz = "1600";
        accel-fs = <8>;
        status = "okay";

        int1-map-io = <0x01>;   /* abilita data-ready su INT1 */
        int1-conf-io = <0x0A>;  /* active high, push-pull */
        int2-map-io = <0x00>;   /* INT2 non usato: disabilita mappature */
        int2-conf-io = <0x00>;
    };
};

/* Partizione per le impostazioni persistenti (settings FCB) */
&w25q64jvxgim {
    partitions {
        compatible = "fixed-partitions";
        #address-cells = <1>;
        #size-cells = <1>;

        /* Partizione per le impostazioni */
        storage_partition: partition@7c0000 {
            label = "storage";
            reg = <0x007c0000 0x00040000>; /* 256 KB */
        };
    };
};